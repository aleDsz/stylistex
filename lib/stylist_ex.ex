defmodule StylistEx do
  @moduledoc """
  StylistEx is a parser for CSS to Elixir types and structs,
  which can generate CSS string with validations that are triggered
  before "compiling the CSS".
  """
end
